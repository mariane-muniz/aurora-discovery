spring:
  application:
    name: registry
  cloud:
    config:
      uri
server:
  port: 8081
eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:${server.port}/eureka